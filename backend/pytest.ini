[pytest]
# src/ on the path so tests import handlers exactly the way Lambda does
# (CodeUri: src/, so `handlers` and `common` are both top-level packages).
pythonpath = src
testpaths = tests
addopts = -q --cov=src --cov-report=term-missing
